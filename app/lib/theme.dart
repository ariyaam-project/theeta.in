import 'package:flutter/material.dart';

/// Palette mirrored from the web app (web/pages/index.vue).
const ink = Color(0xFF2C2B3A); // navy text
const paper = Color(0xFFFFFFFF); // cards
const bg = Color(0xFFF6F5FB); // app background (lavender white)
const accent = Color(0xFF8B80D6); // primary purple
const accent2 = Color(0xFFA99FF0); // lighter purple
const softPurple = Color(0xFFECE9FB); // lavender soft
const muted = Color(0xFF6B6A7C); // muted text
const peach = Color(0xFFF3A18B); // warm accent
const gold = Color(0xFFF3A18B); // legacy name → peach
