import 'package:flutter/material.dart';

/// Theta brutalist palette — shared across pages/widgets.
const ink = Color(0xFF2F231A);
const paper = Color(0xFFFFF8EC);
const accent = Color(0xFFE1306C);
const gold = Color(0xFFF7AB3F);
const bg = Color(0xFFFFF4E0);
